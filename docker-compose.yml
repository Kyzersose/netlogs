services:
  rsyslog:
    build: .
    container_name: syslog-server
    restart: always
    ports:
      - "514:514/udp"
      - "514:514/tcp"
    volumes:
      - ./logs:/var/log
    environment:
      - TZ=America/New_York
